<% provide(:head_title, @page.menu_title) %>

<%= render partial: "site/orientations/orientation_list", locals: {
  orientations: @orientations,
  page: @page,
  translation_key: 'site.pages.professors.in_tcc_one.total'
} %>