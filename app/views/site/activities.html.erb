<% provide(:head_title, @page.menu_title) %>

<h1 class="page-title mb-5">
  <%= t('views.title.calendar.tcc.one', calendar: @calendar_tcc_one.year_with_semester) %>
</h1>

<%= render('site/activities/table', activities: @activities_tcc_one) %>

<h1 class="page-title mb-5">
  <%= t('views.title.calendar.tcc.one', calendar: @calendar_tcc_two.year_with_semester) %>
</h1>

<%= render('site/activities/table', activities: @activities_tcc_two) %>
