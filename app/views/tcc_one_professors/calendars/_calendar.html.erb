<tr>
  <td>
    <%= link_to calendar.year_with_semester, tcc_one_professors_calendar_activities_path(calendar) %>
  </td>
  <td>
    <%= link_to(tcc_one_professors_calendar_orientations_path(calendar)) do %>
      Visualizar orientações
    <% end %>
  </td>
  <td><%= short_date(calendar.created_at) %></td>
</tr>
