<tr>
  <td>
    <%= link_to(activity.name, tcc_one_professors_calendar_orientation_activity_path(@calendar, @orientation, activity)) %>
  </td>
  <%= render 'shared/orientation_activities/tbody', activity: activity %>
</tr>
