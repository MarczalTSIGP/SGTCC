<% provide(:path, responsible_calendar_activities_tcc_one_path(@calendar, @activity)) %>

<% provide(:page_title, t('breadcrumbs.tcc.one.edit')) %>
<% provide(:head_title, t('breadcrumbs.tcc.one.edit')) %>

<%= render 'responsible/activities/form',
  tcc: @activity.tcc,
  back_url: responsible_calendar_activities_tcc_one_index_path(@calendar) %>
