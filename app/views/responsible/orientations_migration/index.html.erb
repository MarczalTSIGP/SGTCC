<% provide(:page_title, t('breadcrumbs.orientations.migration')) %>
<% provide(:head_title, t('breadcrumbs.orientations.migration')) %>

<%= render Orientations::TableComponent.new(
  orientations: @orientations, 
  namespace: :responsible,
  show_legend: false,
  show_actions: true,
  action_partial: 'responsible/orientations_migration/migration_action'
) %>