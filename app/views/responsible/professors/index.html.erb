<% provide(:page_title, t('breadcrumbs.professors.index')) %>
<% provide(:head_title, t('breadcrumbs.professors.index')) %>

<div class="row">
  <div class="col-8">
    <search url="<%= responsible_professors_search_path %>" search-term="<%= params[:term] %>" />
  </div>
  <div class="col-4">
    <%= link_to(new_responsible_professor_path, class: 'btn btn-outline-primary d-block') do %>
      <i class="fe fe-plus"></i>
      <%= t('breadcrumbs.professors.new') %>
    <% end %>
  </div>
</div>

<div class="text-right mt-3">
  <%= page_entries_info @professors %>
</div>

<div class="table-striped mt-3 table-responsive w-100">
  <table class="table card-table table-vcenter text-nowrap">
    <thead>
      <tr>
        <th><%= Professor.human_attribute_name('name') %></th>
        <th><%= Professor.human_attribute_name('email') %></th>
        <th><%= Professor.human_attribute_name('username') %></th>
        <th><%= Professor.human_attribute_name('created_at') %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <tr>
        <%= render('shared/no_results') if @professors.empty? %>
      </tr>
      <%= render @professors %>
    </tbody>
  </table>
</div>

<%= paginate @professors %>
