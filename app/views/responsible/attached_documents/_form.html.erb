<%= simple_form_for [:responsible, @attached_document], html: { multipart: true } do |f| %>
  <div class="row form-inputs">
    <div class="col-md-6">
      <%= f.input :name %>
    </div>
    <div class="col-md-6">
      <strong><%= f.label :file %></strong>
      <file-input
        name="attached_document[file]"
        id="attached_document_file"
        label="<%= AttachedDocument.human_attribute_name('file') %>"
        :errors="<%= @attached_document.errors[:file] %>">
      </file-input>
    </div>
  </div>

  <div class="d-flex">
    <%= link_to t('simple_form.buttons.back'),
      responsible_attached_documents_path,
      class: 'btn btn-secondary' %>

    <%= f.button :submit,
      t('views.buttons.save'),
      class: 'btn btn-primary ml-auto' %>
  </div>
<% end %>
