<% provide(:page_title, t('breadcrumbs.pages.order')) %>
<% provide(:head_title, t('breadcrumbs.pages.order')) %>

<%= render Pages::OrderListComponent.new(
  update_sidebar_url: responsible_update_sidebar_path
) %>
