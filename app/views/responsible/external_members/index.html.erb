<% provide(:page_title, t('breadcrumbs.external_members.index')) %>
<% provide(:head_title, t('breadcrumbs.external_members.index')) %>

<div class="row">
  <div class="col-8">
    <search url="<%= responsible_external_members_search_path %>" search-term="<%= params[:term] %>" />
  </div>
  <div class="col-4">
    <%= link_to(new_responsible_external_member_path, class: 'btn btn-outline-primary d-block') do %>
      <i class="fe fe-plus"></i>
      <%= t('breadcrumbs.external_members.new') %>
    <% end %>
  </div>
</div>

<div class="text-right mt-3">
  <%= page_entries_info @external_members %>
</div>

<div class="table-striped mt-3 table-responsive w-100">
  <table class="table card-table table-vcenter text-nowrap">
    <thead>
      <tr>
        <th><%= ExternalMember.human_attribute_name('name') %></th>
        <th><%= ExternalMember.human_attribute_name('email') %></th>
        <th><%= ExternalMember.human_attribute_name('created_at') %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <tr>
        <%= render('shared/no_results') if @external_members.empty? %>
      </tr>
      <%= render @external_members %>
    </tbody>
  </table>
</div>

<%= paginate @external_members %>
