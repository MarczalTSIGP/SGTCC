<% provide(:page_title) do %>
  <%= t("breadcrumbs.orientations.tcc.#{@orientation.calendar.tcc}.show",
        calendar: @orientation.calendar.year_with_semester) %>
<% end %>
<% provide(:head_title) do %>
  <%= t("breadcrumbs.orientations.tcc.#{@orientation.calendar.tcc}.show",
        calendar: @orientation.calendar.year_with_semester) %>
<% end %>

<%= provide(:edit_button) do %>
  <%= link_to t('simple_form.buttons.edit'),
    edit_responsible_orientation_path(@orientation),
    class: 'btn btn-primary ml-auto' %>
<% end %>

<%= provide(:orientation_options) do %>
  <div class="mt-2">
    <orientation-renew
      :id="<%= @orientation.id %>"
      label="<%= Orientation.human_attribute_name('renewal_justification') %>"
      :has-permission="<%= @orientation.can_be_renewed?(current_professor) %>"
      error-message="<%= t('errors.messages.blank') %>" />
  </div>

  <div class="mt-3">
    <orientation-cancel
      :id="<%= @orientation.id %>"
      label="<%= Orientation.human_attribute_name('cancellation_justification') %>"
      error-message="<%= t('errors.messages.blank') %>"
      :has-permission="<%= @orientation.can_be_canceled?(current_professor) %>" />
  </div>

  <div class="clearfix"></div>
<% end %>

<%= render('shared/orientations/show', back_url: @back_url) %>
