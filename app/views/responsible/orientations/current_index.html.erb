<% provide(:page_title, @title) %>
<% provide(:head_title, @title) %>

<%= render('responsible/orientations/head') %>

<% cache(@orientations) do %>
  <%= render Orientations::TableComponent.new(orientations: @orientations, namespace: :responsible) %>
<% end %>
