<% provide(:edit_button) do %>
  <%= link_to t('simple_form.buttons.edit'),
    edit_responsible_calendar_activity_path(@calendar, @activity),
    class: 'btn btn-primary ml-auto' %>
<% end %>

<%= render 'shared/activities/show', back_url: responsible_calendar_activities_path(@calendar) %>
