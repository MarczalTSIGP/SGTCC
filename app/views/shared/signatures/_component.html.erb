<% if @signature.term_of_commitment? %>
  <term-of-commitment
    orientation-title="<%= @signature.orientation.title %>"
    orientation-date="<%= document_date(@signature.orientation.created_at) %>"
    document-title="<%= @signature.document.document_type.name %>"
    url-signatures-mark="<%= signatures_mark_path(@signature) %>"
    advisor-label="<%= t("signatures.advisor.labels.#{@signature.orientation.advisor.gender}") %>"
    :signed="<%= @signature.status %>"
    :academic="<%= @signature.orientation.academic.to_json %>"
    :advisor="<%= @signature.orientation.advisor.to_json %>"
    :advisor-scholarity="<%= @signature.orientation.advisor.scholarity.to_json %>"
    :institution="<%= @signature.orientation.institution.to_json %>"
    :institution-responsible="<%= @signature.orientation.institution&.external_member.to_json %>"
    :professor-supervisors="<%= @signature.orientation.professor_supervisors.to_json %>"
    :external-member-supervisors="<%= @signature.orientation.external_member_supervisors.to_json %>" />
<% end %>
