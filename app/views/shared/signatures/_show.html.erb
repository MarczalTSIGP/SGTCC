<% provide(:head_title, t('breadcrumbs.signatures.show')) %>
<% provide(:page_title) do %>
  <signature-show-title />
<% end %>

<div class="clearfix mb-4">
  <div class="float-right ml-2">
    <signature-download-button
      head-title="<%= t('breadcrumbs.signatures.show') %>"
      document-title="<%= @signature.document_filename %>" />
  </div>

  <% unless @signature.status %>
    <div>
      <signature-confirm
        label="<%= label %>"
        url="<%= confirm_url %>" />
    </div>

    <div class="float-right">
      <signature-button
        show-title="<%= t('breadcrumbs.signatures.TCO.show',
                           academic: @signature.orientation.academic.name) %>" />
    </div>
  <% end %>
</div>

<div class="container">
  <div>
    <%= render 'shared/signatures/component', signature: @signature %>
  </div>
  <div>
    <signature-status
      url="<%= document_status_path(@signature.document) %>" />
  </div>
</div>

<div class="d-flex">
  <%= link_to t('simple_form.buttons.back'), back_url, class: 'btn btn-secondary' %>
</div>

