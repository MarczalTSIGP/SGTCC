<% unless @examination_boards.empty? %>
  <div class="card col-12">
    <div class="card-header">
      <h3 class="card-title">
        <b><%= t('breadcrumbs.examination_boards.recent') %></b>
      </h3>
    </div>
    <div class="table-sm table-responsive">
      <%= render ExaminationBoards::TableComponent.new(examination_boards: @examination_boards, namespace: :tcc_one_professors) %>
    </div>
  </div>
<% end %>

