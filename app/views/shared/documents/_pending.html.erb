<% provide(:page_title, t('breadcrumbs.signatures.pendings')) %>
<% provide(:head_title, t('breadcrumbs.signatures.pendings')) %>

<%= render 'shared/documents/table', entry_name: t('breadcrumbs.signatures.pendings') %>
