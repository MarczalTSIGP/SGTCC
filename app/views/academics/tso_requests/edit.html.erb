<% provide(:page_title, t('breadcrumbs.documents.requests.tso.new')) %>
<% provide(:head_title, t('breadcrumbs.documents.requests.tso.new')) %>

<%= render 'form', url: academics_tso_request_path(@document) %>


