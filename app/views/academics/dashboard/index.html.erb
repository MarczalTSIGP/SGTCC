<% provide(:page_title, "Dashboard") %>
<% provide(:head_title, "Dashboard") %>

<div class="card">
  <div class="card-header">
    <h3 class="card-title">
      <%= t('breadcrumbs.documents.recent.pending') %>
    </h3>
  </div>
  <div class="table-responsive">
    <%= render 'shared/documents/table' %>
  </div>
</div>

<div class="card">
  <div class="card-header">
    <h3 class="card-title">
      <%= t('breadcrumbs.meetings.recent') %>
    </h3>
  </div>
  <div class="table-responsive">
    <%= render 'shared/meetings/table' %>
  </div>
</div>
