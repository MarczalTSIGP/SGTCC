<% provide(:page_title, t('breadcrumbs.documents.requests.tep.new')) %>
<% provide(:head_title, t('breadcrumbs.documents.requests.tep.new')) %>

<%= render 'form', url: academics_tep_request_path(@document) %>


