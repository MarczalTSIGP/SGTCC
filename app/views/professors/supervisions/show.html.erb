<% provide(:page_title) do %>
  <%= t("breadcrumbs.supervisions.tcc.#{@orientation.current_calendar.tcc}.show",
        calendar: @orientation.current_calendar.year_with_semester) %>
<% end %>
<% provide(:head_title) do %>
  <%= t("breadcrumbs.supervisions.tcc.#{@orientation.current_calendar.tcc}.show",
        calendar: @orientation.current_calendar.year_with_semester) %>
<% end %>

<%= render('shared/orientations/show', back_url: @back_url) %>

