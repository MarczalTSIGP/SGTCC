<% provide(:page_title, t('breadcrumbs.signatures.show')) %>
<% provide(:head_title, t('breadcrumbs.signatures.show')) %>

<div class="card">
  <div class="card-body">
    <%= render 'shared/signatures/term_of_commitment' %>
  </div>
</div>

<div class="d-flex">
  <%= link_to t('simple_form.buttons.back'),
    professors_signatures_pending_path,
    class: 'btn btn-secondary' %>
</div>

