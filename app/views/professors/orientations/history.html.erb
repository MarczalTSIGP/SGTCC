<% provide(:page_title, t('breadcrumbs.orientations.history')) %>
<% provide(:head_title, t('breadcrumbs.orientations.history')) %>

<div class="row">
  <div class="col-sm-12 col-lg-8">
    <%= render 'shared/orientations/search', url: professors_orientations_search_history_path, search_term: params[:term] %>
  </div>
</div>

<%= render(
  Orientations::TableComponent.new(
    orientations: @orientations,
    namespace: namespace
  )
) %>
