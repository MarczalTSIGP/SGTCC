name: CI
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  tests:
    uses: ./.github/workflows/_tests.yml

  code_quality:
    uses: ./.github/workflows/_linters.yml
